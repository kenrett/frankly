#!/usr/bin/env ruby
# frozen_string_literal: true

ENV["BUNDLE_GEMFILE"] ||= File.expand_path("../Gemfile", __dir__)
require "bundler/setup"
require "frankly"

begin
  require "pry"
  Pry.start
rescue LoadError
  require "irb"
  IRB.start(__FILE__)
end
